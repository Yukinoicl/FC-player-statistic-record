package com.fcdata.fcdataserver.repository;

import com.fcdata.fcdataserver.entity.CareerTotal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CareerTotalRepository extends JpaRepository<CareerTotal, Long> {
}
